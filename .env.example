# Optional shell setting. The application does not auto-load .env files.
FRAUD_LOG_LEVEL=INFO

# Optional Streamlit database path, relative to the current working directory.
FRAUD_DATABASE_PATH=data/processed/paysim.duckdb

# Reserved for the later summary component; unused in this version.
OPENAI_API_KEY=
